# 🧠 Учебный проект: Валидация и Генерация

Небольшой, но полезный учебный проект на Python, демонстрирующий работу с регулярными выражениями, генерацией случайных данных и математическими операциями. Проект содержит две независимые, но интересные функции.

## ✨ Функционал

Проект реализует две ключевые функции:

1.  **`validate_email(email: str) -> bool`**
    *   Проверяет корректность переданного email-адреса с помощью регулярного выражения.
    *   Возвращает `True`, если email валиден, и `False` — если нет.

2.  **`generate_random_multiple_of_three(start: int, end: int) -> int`**
    *   Генерирует случайное число в заданном пользователем диапазоне [`start`, `end`].
    *   **Гарантированно** возвращает число, кратное трём.
    *   Если в диапазоне нет чисел, кратных трём, функция возвращает `None` с соответствующим сообщением.

## 🚀 Быстрый старт

### Предварительные требования

*   Установленный **Python 3.7** или выше.
*   Утилита `pip` для установки зависимостей.

### Установка и запуск

1.  **Клонируйте репозиторий (или скопируйте файлы проекта):**
    ```bash
    git clone https://github.com/ваш-username/your-cool-project.git
    cd your-cool-project
    ```

2.  **(Опционально) Создайте виртуальное окружение (рекомендуется):**
    ```bash
    python -m venv venv
    # Для Linux/macOS:
    source venv/bin/activate
    # Для Windows:
    # venv\Scripts\activate
    ```

3.  **Установите зависимости:**
    *В данном проекте используются только модули стандартной библиотеки Python (`re`, `random`), поэтому файл `requirements.txt` может отсутствовать. Если вы добавите внешние зависимости, создайте его.*

4.  **Запустите пример использования:**
    ```bash
    python main.py
    ```
    *Если у вас нет `main.py`, создайте его, как показано в разделе **«Использование»** ниже.*

## 📖 Использование

Скопируйте код ниже в файл `main.py` или в интерактивную сессию Python.

```python
# Импорт функций из нашего модуля
# Предполагается, что основной код находится в файле `core.py`
from core import validate_email, generate_random_multiple_of_three

# --- Демонстрация работы валидации email ---
print("=" * 50)
print("ДЕМОНСТРАЦИЯ ВАЛИДАЦИИ EMAIL")
print("=" * 50)

test_emails = [
    "user@example.com",
    "hello.world@subdomain.co.uk",
    "invalid-email@",
    "no.at.sign.com",
    "with_space @domain.com",
]

for email in test_emails:
    result = validate_email(email)
    status = "✅ ВАЛИДЕН" if result else "❌ НЕВАЛИДЕН"
    print(f"{email:35} -> {status}")

# --- Демонстрация работы генератора чисел ---
print("\n" + "=" * 50)
print("ДЕМОНСТРАЦИЯ ГЕНЕРАЦИИ ЧИСЕЛ, КРАТНЫХ 3")
print("=" * 50)

test_ranges = [
    (1, 10),     # Должно сработать
    (5, 8),      # Должно сработать (6 подходит)
    (1, 5),      # Должно сработать (3 подходит)
    (2, 4),      # Нет кратных трём (вернёт None)
]

for start, end in test_ranges:
    result = generate_random_multiple_of_three(start, end)
    print(f"Диапазон [{start:3}, {end:3}]: {result}")
    здать простой веб-интерфейс с помощью Flask или Streamlit.

    Графики: Визуализировать распределение сгенерированных чисел, кратных трём, с помощью matplotlib.

    Логирование: Добавить запись результатов работы функций в файл лога.

📄 Лицензия

Этот учебный проект распространяется под лицензией MIT. Подробнее см. в файле LICENSE (если вы его создадите).
text


---

**Совет:** Чтобы проект выглядел ещё профессиональнее:
1.  Создайте файл `.gitignore` для Python (шаблон можно взять на [github.com/github/gitignore](https://github.com/github/gitignore)).
2.  Добавьте простые модульные тесты в папку `tests/`.
3.  Создайте виртуальное окружение и, если появятся зависимости, файл `requirements.txt`.
4.  Залейте проект на GitHub, GitLab или другой хостинг — ссылку вставьте в строку клонирования в README.

Удачи с проектом! Он действительно звучит интересно

